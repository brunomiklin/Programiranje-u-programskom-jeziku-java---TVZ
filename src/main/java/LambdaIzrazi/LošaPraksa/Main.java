package LambdaIzrazi.LošaPraksa;

public class Main {
    static void main() {
        // ❌ LOŠE - side effect
//        int count = 0;
//        users.stream()
//                .filter(u -> u.isActive())
//                .forEach(u -> count++);  // NE RADI! Kompajler error

        // Variable used in lambda expression should be final or effectively final
    }
}
